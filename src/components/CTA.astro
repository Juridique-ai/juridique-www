---
const appUrl = "https://aoli-juridique-prod-frontend.azurewebsites.net";
---

<section class="py-24 relative overflow-hidden">
  <!-- Background glow -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-secondary/10 pointer-events-none"></div>

  <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Mission statement -->
    <h2 class="headline-lg mb-6">
      Le droit pour <span class="text-gradient text-glow">tous</span>.<br/>
      Pas que pour les riches.
    </h2>

    <p class="text-xl sm:text-2xl text-text-muted max-w-2xl mx-auto mb-12 italic">
      "L'accès au droit est un droit fondamental.<br/>
      Nous le rendons enfin accessible."
    </p>

    <!-- CTA Box -->
    <div class="relative inline-block">
      <div class="absolute inset-0 bg-gradient-to-r from-primary to-secondary rounded-3xl blur-xl opacity-50 animate-pulse-scale"></div>
      <div class="relative bg-gradient-to-r from-primary to-secondary rounded-3xl p-10 sm:p-12">
        <h3 class="text-2xl sm:text-3xl font-bold text-white mb-4">
          Essayez maintenant
        </h3>
        <p class="text-white/90 mb-8 max-w-md mx-auto">
          Le produit est live. Testez-le gratuitement.<br/>
          Connexion Microsoft sécurisée.
        </p>
        <a
          href={appUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-3 px-10 py-5 rounded-full bg-white text-background font-bold text-lg hover:scale-105 transition-transform"
        >
          Accéder à l'application
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12" />
            <polyline points="12 5 19 12 12 19" />
          </svg>
        </a>
      </div>
    </div>

    <!-- Trust badges -->
    <div class="mt-16 flex flex-wrap items-center justify-center gap-6 text-text-muted text-sm">
      <div class="flex items-center gap-2">
        <span class="text-primary">✓</span>
        Connexion Microsoft sécurisée
      </div>
      <div class="flex items-center gap-2">
        <span class="text-primary">✓</span>
        Hébergé en Europe
      </div>
      <div class="flex items-center gap-2">
        <span class="text-primary">✓</span>
        Disponible 24h/24
      </div>
      <div class="flex items-center gap-2">
        <span class="text-primary">✓</span>
        Aucune carte requise
      </div>
    </div>
  </div>
</section>
