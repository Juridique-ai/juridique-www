---
const moats = [
  {
    title: "100% Européen",
    subtitle: "Zero Cloud Act",
    description: "Hébergé en Suède. Vos données ne quittent JAMAIS l'Europe.",
    icon: "🇪🇺",
  },
  {
    title: "Zero Data",
    subtitle: "Rien stocké",
    description: "Vos documents traités en mémoire. Effacés instantanément.",
    icon: "🔐",
  },
  {
    title: "RGPD + AI Act",
    subtitle: "Natif",
    description: "Conforme dès la conception. Pas un patch post-lancement.",
    icon: "✅",
  },
  {
    title: "5 Juridictions",
    subtitle: "Dès le lancement",
    description: "FR, BE, LU, DE, CH. Aucun concurrent ne couvre ça.",
    icon: "🌍",
  },
];
---

<section id="conformite" class="py-24 relative overflow-hidden">
  <!-- Background accent -->
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-primary/5 to-transparent pointer-events-none"></div>

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="headline-lg mb-6">
        Pourquoi <span class="text-gradient">nous</span><br/>
        et pas les autres?
      </h2>
    </div>

    <!-- Moats List -->
    <div class="max-w-3xl mx-auto space-y-6">
      {moats.map((moat) => (
        <div class="flex items-start gap-6 p-6 rounded-2xl glass-light hover:glow-sm transition-all">
          <!-- Check icon -->
          <div class="w-14 h-14 rounded-full bg-primary flex items-center justify-center flex-shrink-0 text-2xl">
            {moat.icon}
          </div>

          <!-- Content -->
          <div class="flex-1">
            <div class="flex items-center gap-3 mb-1">
              <h3 class="text-xl font-bold">{moat.title}</h3>
              <span class="text-sm text-primary font-medium">— {moat.subtitle}</span>
            </div>
            <p class="text-text-muted">{moat.description}</p>
          </div>
        </div>
      ))}
    </div>

    <!-- Trust metrics -->
    <div class="mt-16 grid grid-cols-2 sm:grid-cols-4 gap-4 max-w-3xl mx-auto">
      <div class="text-center p-6 rounded-2xl glass-light">
        <div class="text-3xl sm:text-4xl font-bold text-primary">4</div>
        <div class="text-sm text-text-muted">Modules IA</div>
      </div>
      <div class="text-center p-6 rounded-2xl glass-light">
        <div class="text-3xl sm:text-4xl font-bold text-primary">30s</div>
        <div class="text-sm text-text-muted">Temps réponse</div>
      </div>
      <div class="text-center p-6 rounded-2xl glass-light">
        <div class="text-3xl sm:text-4xl font-bold text-primary">28€</div>
        <div class="text-sm text-text-muted">/ mois infra</div>
      </div>
      <div class="text-center p-6 rounded-2xl glass-light">
        <div class="text-3xl sm:text-4xl font-bold text-primary">Live</div>
        <div class="text-sm text-text-muted">En prod</div>
      </div>
    </div>
  </div>
</section>
