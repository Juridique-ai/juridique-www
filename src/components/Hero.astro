---
const appUrl = "https://aoli-juridique-prod-frontend.azurewebsites.net";

const stats = [
  { value: "5", label: "Modules spécialisés" },
  { value: "5", label: "Juridictions EU" },
  { value: "100%", label: "Hébergé en Europe" },
];
---

<section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden bg-gradient-radial">
  <!-- Background decoration -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-1/4 -left-32 w-96 h-96 bg-primary/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-1/4 -right-32 w-96 h-96 bg-secondary/10 rounded-full blur-3xl"></div>
  </div>

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full glass-light text-sm text-text-muted mb-8 animate-fade-in">
        <span class="w-2 h-2 rounded-full bg-primary animate-pulse"></span>
        <span>Conforme RGPD & AI Act</span>
      </div>

      <!-- Headline -->
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight mb-6 animate-fade-in stagger-1">
        Assistance juridique
        <span class="text-gradient">intelligente</span>
        <br />pour l'Europe
      </h1>

      <!-- Subheadline -->
      <p class="text-lg sm:text-xl text-text-muted max-w-2xl mx-auto mb-10 animate-fade-in stagger-2">
        Analysez vos contrats, créez votre entreprise, obtenez des conseils juridiques.
        Une plateforme IA couvrant 5 juridictions européennes.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-16 animate-fade-in stagger-3">
        <a
          href={appUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-primary text-background font-semibold hover:bg-primary-light transition-all hover:scale-105 glow"
        >
          Commencer maintenant
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12" />
            <polyline points="12 5 19 12 12 19" />
          </svg>
        </a>
        <a
          href="#modules"
          class="inline-flex items-center gap-2 px-6 py-3 rounded-xl glass-light font-semibold hover:bg-white/10 transition-all"
        >
          Découvrir les modules
        </a>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-3 gap-8 max-w-lg mx-auto animate-fade-in stagger-4">
        {stats.map((stat) => (
          <div class="text-center">
            <div class="text-2xl sm:text-3xl font-bold text-primary">{stat.value}</div>
            <div class="text-sm text-text-muted">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-float">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-text-muted" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <polyline points="6 9 12 15 18 9" />
    </svg>
  </div>
</section>
